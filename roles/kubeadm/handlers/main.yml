-   name: reload sysctl
    shell: sysctl --system

-   name: reload mount
    shell: mount -a

-   name: reboot
    reboot:
        reboot_timeout: 300

# Local Variables:
# mode: yaml
# tab-width: 4
# End:
