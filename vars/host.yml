host:
    hems:
        mode: netserver
        model: bl75t
        monitor: bl75t@hems

    worker:
        mode: netserver
        model: bl50t
        monitor: bl75t@hems

    database:
        mode: netclient
        monitor: bl75t@hems
